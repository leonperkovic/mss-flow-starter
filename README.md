# Mars Scavenging Station Flow

## Setup
1. Build project:
    ```
   ./gradlew clean build
   ```
2. Run with default profiles

Webapp available at: http://localhost:8080/mss-flow/camunda

Camunda REST API available at: http://localhost:8080/mss-flow/engine-rest
